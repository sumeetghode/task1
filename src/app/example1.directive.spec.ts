import { Example1Directive } from './example1.directive';

describe('Example1Directive', () => {
  it('should create an instance', () => {
    const directive = new Example1Directive();
    expect(directive).toBeTruthy();
  });
});
